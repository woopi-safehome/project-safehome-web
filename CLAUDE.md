@AGENTS.md

# CLAUDE.md — project-safehome-web

## 역할과 경계

등기부등본 분석 서비스의 **웹 프론트**. 비로그인으로도 분석할 수 있고, 로그인하면 이력이 남는다.

**여기서 하지 않는 일**

- **AI 분석 서버를 직접 부르지 않는다.** 통신 대상은 백엔드 하나뿐이다.
- **판정을 다시 계산하지 않는다.** 위험도·검증 결과는 서버가 정하고 화면은 받아서 보여준다.
- **서버 비밀값을 두지 않는다.** 브라우저에 내려가는 값은 전부 공개된 것으로 다룬다.

## 구조

`app`(라우팅) → `features`(기능) → `shared`(공용). **의존은 이 한 방향뿐이고 ESLint 가 막는다.**
기능끼리 가로로 참조하는 것도 막힌다 — 같은 기능 안은 상대경로, 기능 사이에 공유할 것은 `shared` 로 올린다.
외울 필요는 없다. 어기면 lint 가 이유와 함께 알려준다.

## 필수 절차

- **응답·문서·주석은 한국어로 쓴다.** 코드 식별자는 영문.
- **서버 응답 형식을 여기서 정하지 않는다.** 원본은 api 저장소 README 의 계약 절이다.
  확인할 수 없으면 실제 응답으로 확인하고, **모르는 필드가 와도 무시하고 동작**해야 한다.
  서버가 이 앱보다 먼저 배포된다.
- **끝내기 전에 `npm run lint` 와 `npm run build` 를 둘 다 돌린다.** lint 가 구조를, build 가 타입을 본다.
  한쪽만으로는 다른 쪽이 깨진 걸 모른다.
